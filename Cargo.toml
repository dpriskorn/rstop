[package]
name = "htop_zram"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
sysinfo = "0.32"
libc = "0.2"
tabled = "0.20"
chrono = "0.4"

[dev-dependencies]
# clippy is included with Rust, run with: cargo clippy